% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hierarchy_recipe.R
\name{hierarchy_recipe}
\alias{hierarchy_recipe}
\title{Compile a hierarchy transformation recipe}
\usage{
hierarchy_recipe(
  order,
  compress,
  decomposition = NULL,
  post_compress = NULL,
  name_prefix = "",
  innovation_suffix = TRUE
)
}
\arguments{
\item{order}{Character vector of stage names, in residualization order.}

\item{compress}{A \code{\link{block_pca}} specification applied to each
raw stage before decomposition.}

\item{decomposition}{An \code{\link{ordered_innovation}} specification, or
\code{NULL} to emit compressed scores only.}

\item{post_compress}{Optional \code{\link{block_pca}} applied to each
innovation (or compressed score if \code{decomposition} is \code{NULL}).}

\item{name_prefix}{Optional prefix for output band names, for example
\code{"vgg"} yields \code{vgg_b1}, \code{vgg_b2_innovation}, ...}

\item{innovation_suffix}{Logical; if \code{TRUE} (default), stages after
the first are named \code{*_innovation} when a decomposition is present.}
}
\value{
An object of class \code{hierarchy_recipe}.
}
\description{
A recipe records stage order, per-stage compression, an optional
innovation decomposition, and an optional post-residual compression.
}
\examples{
hierarchy_recipe(
  order = c("b1", "b2"),
  compress = block_pca(rank = 8),
  decomposition = ordered_innovation(method = "svd")
)
}
