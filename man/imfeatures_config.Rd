% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imfeatures_config.R, R/zzz.R
\name{imfeatures_config}
\alias{imfeatures_config}
\alias{install_imfeatures_python}
\title{Configure the Python environment for imfeatures}
\usage{
imfeatures_config(
  envname = "r-imfeatures",
  method = c("auto", "conda", "virtualenv", "existing"),
  reset = FALSE,
  create = TRUE
)

install_imfeatures_python(...)
}
\arguments{
\item{envname}{Name of the environment to create/use. Defaults to
"r-imfeatures".}

\item{method}{Installation method. Options: "auto", "conda", "virtualenv", 
or "existing" (to use current Python without modification).}

\item{reset}{Force reinstallation of the environment.}

\item{create}{Whether to create the environment if it doesn't exist.
Set to FALSE on HPC systems where you want to use a pre-configured environment.}

\item{...}{Arguments passed to imfeatures_config}
}
\value{
Invisible path to the configured Python binary.
}
\description{
Sets up or activates the Python environment required by the package.
The environment is detected once per R session and cached.  Users can
force a reinstall with `reset = TRUE`.
}
\section{Functions}{
\itemize{
\item \code{install_imfeatures_python()}: Deprecated alias for imfeatures_config

}}
\section{HPC recommendation}{

On HPC systems, prefer using an existing Python (module + virtualenv) and
avoid auto environment creation. Set `IMFEATURES_SKIP_PYTHON=TRUE` to skip
auto-configuration on package load and call [use_existing_python()] to point
to your Python. You can also set `RETICULATE_PYTHON` in `~/.Renviron` so it is
used automatically. If you do use `imfeatures_config()` on HPC, set
`IMFEATURES_METHOD=virtualenv` to avoid Conda.
}

