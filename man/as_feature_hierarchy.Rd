% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hierarchy_blocks.R
\name{as_feature_hierarchy}
\alias{as_feature_hierarchy}
\title{Build a named feature-hierarchy object}
\usage{
as_feature_hierarchy(
  blocks,
  sample_id = NULL,
  stage_order = NULL,
  schema = NULL,
  provenance = NULL
)
}
\arguments{
\item{blocks}{Named list of numeric matrices, each with the same number of
rows (observations). A \code{feature_hierarchy} is returned unchanged.}

\item{sample_id}{Optional vector of sample identifiers of length
\code{nrow}. If omitted, row names of the first block are used, otherwise
\code{seq_len(nrow)}.}

\item{stage_order}{Optional character vector giving the intended stage
order. Must be a permutation of \code{names(blocks)}. Defaults to the
list order.}

\item{schema}{Optional list describing column-name conventions or pooling.}

\item{provenance}{Optional list of extraction metadata.}
}
\value{
An object of class \code{feature_hierarchy} with elements
  \code{blocks}, \code{sample_id}, \code{stage_order}, \code{schema}, and
  \code{provenance}.
}
\description{
Wraps a named list of aligned feature matrices (one per stage) into an
object that records sample identifiers, stage order, and optional schema
or provenance metadata.
}
\examples{
blocks <- list(
  b1 = matrix(rnorm(20), 10, 2),
  b2 = matrix(rnorm(30), 10, 3)
)
as_feature_hierarchy(blocks)
}
