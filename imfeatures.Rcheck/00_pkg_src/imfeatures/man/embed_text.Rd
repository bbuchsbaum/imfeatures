% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embed_text.R
\name{embed_text}
\alias{embed_text}
\title{Compute text embeddings}
\usage{
embed_text(
  text,
  backend = c("openai", "gemini", "hf"),
  model = NULL,
  dimensions = NULL,
  task_type = NULL,
  normalize = FALSE,
  api_key = NULL
)
}
\arguments{
\item{text}{Character string or vector of strings to embed.}

\item{backend}{Embedding backend to use: "openai", "gemini", or "hf".}

\item{model}{Model name specific to the backend.}

\item{dimensions}{Optional dimension specification (backend-specific).}

\item{task_type}{Optional task type (Gemini only).}

\item{normalize}{Logical; whether to normalize embeddings to unit length.}

\item{api_key}{Optional API key override.}
}
\value{
For a single text: numeric vector. For multiple texts: matrix with
  one row per text.
}
\description{
Compute text embeddings
}
\examples{
\dontrun{
# Single text embedding
emb <- embed_text("A beautiful sunset", backend = "openai")

# Multiple texts
texts <- c("First caption", "Second caption")
emb_matrix <- embed_text(texts, backend = "gemini", model = "text-embedding-004")
}

}
