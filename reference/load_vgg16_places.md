# Load VGG16 Places365 model

Loads a VGG16 model pre-trained on the Places365 dataset for scene
recognition.

## Usage

``` r
load_vgg16_places()

vgg16_places()
```

## Value

A Keras model object representing VGG16 trained on Places365
