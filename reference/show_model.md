# Show Model Architecture for thingsvision_extractor

Prints the architecture of the underlying Python model, showing
available layers/modules for feature extraction.

## Usage

``` r
show_model(object, ...)
```

## Arguments

- object:

  An object of class \`thingsvision_extractor\`.

- ...:

  Additional arguments (ignored).

## Value

Invisibly returns the input object.
