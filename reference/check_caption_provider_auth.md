# Check if provider API key is configured

Check if provider API key is configured

## Usage

``` r
check_caption_provider_auth(provider)
```

## Arguments

- provider:

  Provider name.

## Value

Logical indicating if API key is set.
