# Compute similarity between caption embeddings

Compute similarity between caption embeddings

## Usage

``` r
compute_caption_similarity(caption_features, metric = "cosine")
```

## Arguments

- caption_features:

  A tibble from caption_features with embeddings.

- metric:

  Similarity metric (default: "cosine").

## Value

Similarity matrix.
